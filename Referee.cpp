#include "Referee.h"

char Referee::refGame(*Player Player1, *Player Player2)
{
    char Move1 = Player1->makeMove();
    char Move2 = Player2->makeMove();

    // implement logic to determine win - perspective of first player

    
}